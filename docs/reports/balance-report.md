# 三模式 × 三难度平衡审计

> 本文保留调整前的审计结论。后续修复、最终数值和复测结果见 [平衡调整报告](balance-adjustment.md)。基线原始数据保存在 `test/reports/balance-before.json`。

日期：2026-09-17。对象：当前工作区（包含尚未发布的敌人追踪调整），不是已发布 v1.4.1 安装包。

## 结论

**不完全符合难度定位，暂不通过整体平衡验收。** 前期测试能区分简单、中等、困难，但 Boss 缺少难度缩放，肉鸽存在强化失效，后期简单档在固定装备压力场景中容错不足。优先修复实现缺陷，再进行数值调优与真人测试。此次仅增加测试和报告，未更改游戏数值。

## 方法与边界

- 运行实际 core / logic / bosses / entities / input / game 代码，通过 Node VM 替换 DOM、Canvas、音频和时钟；移动、开火、碰撞、伤害、掉落、EMP、选卡和波次推进沿用生产逻辑。渲染函数桩不提供真实视觉判断；game.draw 仍执行相位状态更新。
- 3 模式 × 3 难度 × 2 阶段 × 2 画布 × 2 控制脚本 × 6 种子，共 **432 条**模拟；每条最多 120 秒，步长 1/30 秒。
- 画布为 390×844、900×760；HUD 底边近似为普通战斗 100px、Boss 战 155px，不模拟所有真实设备安全区。
- 控制脚本：底部中央定点；底部按 6 秒周期横移。均自动开火，EMP 冷却完成即用，肉鸽根据实际出现的选项优先攻击成长、低血时优先恢复。没有无敌、秒杀或自动识别安全通道。
- 前期从正常开局开始。后期直接进入无限/肉鸽第 20 波、闯关第 8 关，统一火力 3、伤害 2，**未累计前期道具或肉鸽强化**。后期场景测试固定装备承压能力，不代表自然游玩到该阶段的胜率。
- 每个模式/难度/阶段有 24 条测试（两画布、两脚本、六种子）。下表为到 120 秒仍存活的条数，**不是玩家胜率或通关率**；固定脚本对弹墙过道、掉落收集等机制不够聪明。

## 前期结果

| 模式 | 简单存活 | 中等存活 | 困难存活 | 判断 |
|---|---:|---:|---:|---|
| 无限 | 24/24 | 15/24 | 5/24 | 梯度清晰；中等的画布差异明显 |
| 闯关 | 24/24 | 10/24 | 2/24 | 中等/困难前期比无限更吃紧 |
| 肉鸽 | 24/24 | 16/24 | 5/24 | 前期梯度存在，但成长系统未通过功能核验 |

前期中等：无限手机 12/12、桌面 3/12；闯关手机 6/12、桌面 4/12；肉鸽手机 10/12、桌面 6/12。宽画布使横向移动距离、命中覆盖和收集路径发生变化，当前难度不是纯设备无关。同样的归一化横移脚本在大屏上移动更快，因此这也是控制脚本的混杂因素，不能据此断言手机对所有玩家都更简单。

## 后期固定装备压力测试

| 模式 / 起点 | 简单存活 | 中等存活 | 困难存活 |
|---|---:|---:|---:|
| 无限 / 第20波 | 2/24 | 1/24 | 0/24 |
| 闯关 / 第8关 | 15/24 | 4/24 | 0/24 |
| 肉鸽 / 第20波 | 2/24 | 1/24 | 0/24 |

无限/肉鸽此起点是第 4 个 Boss「移动堡垒」，三个难度均使用基础 300 × 1.6 = 480 HP，装甲受伤 ×0.6、装填受伤 ×1.5。后期两模式的固定装备注入条件相同且大部分未推进下一波，因此其结果相同；不能将此解释为肉鸽长期成长没有作用。测试只能说明这一 Boss 对固定脚本/装备形成明显压力，尤其简单档并没有得到 Boss 层面的降压。

## 确定的实现问题

### P1：敌弹速度倍率配置未实际生效

`js/logic.js` 提供 `bulletSpeedMul`，但 `js/game.js` 的 `fireEnemyBullet` 和敌弹更新未应用它。九种模式/难度组合实测枪手子弹均为 **220 px/s**，没有体现简单/中等/困难的 0.78/1/1.30 倍率。Boss 子弹同样走这个通道。

### P1：肉鸽六种强化选择后失效

对 15 项选卡逐项调用实际 `choosePerk`，比对 `Logic.rogueApply` 的预期结果：9 项生效，6 项失败。`choosePerk` 只复制基础属性，漏掉如下字段。

| 强化 | 字段 | 选择一次的预期 | 实际 |
|---|---|---:|---:|
| 弱点分析 | crit | 1 | 0 |
| 致命一击 | critDmg | 1 | 0.5 |
| 磁力收集 | magnet | 1 | 0 |
| 回旋护板 | reflect | 1 | 0 |
| 虹吸协议 | vamp | 0.04 | 0 |
| 过载线圈 | emp | 0.4 | 0 |

这会使选卡收益依赖随机选项是否恰好属于失效项，破坏肉鸽难度与构筑评价；其中存在封顶的字段一直未增长，还可能反复出现。

### P1：Boss 缺少三档战斗参数梯度

实际首领基础血量：无限/肉鸽首个 Boss 在三档均为 120，闯关首个 Boss 均为 58。`currentBossHp()` 未乘难度系数；`bossAttack()` 的预警、弹速、休整周期仅按身份、攻击轮次和阶段变化。生命数量仍让整体容错有别，召唤单位也有差异，但 Boss 本体的反应压力没有按档位分层。

### P2：计划内出怪间隔没有用于实际生成

闯关 Boss 波配置写有更快的 spawnInterval，但游戏每次生成后用 `currentDiff().spawnInterval` 重置计时，没有采用具体波次配置。肉鸽生成使用 wave+1 的当前难度，而波次计划使用 wave。需要统一“计划”和“执行”的来源，避免用配置表误判真实压力。

## 九种组合的验收判断

| 模式 | 简单 | 中等 | 困难 |
|---|---|---|---|
| 无限 | 前期符合宽容定位；后期 Boss 需降压验证 | 前期有适当压力；设备差异与 Boss 梯度待修 | 明显更难；无法证明当前后期压力足够公平 |
| 闯关 | 前期友好；后期仍需完整十关实玩 | 前期压力高于其他模式；待调整节奏 | 固定脚本前期生存少；需熟练玩家完整通关验证 |
| 肉鸽 | 前期友好；六项强化失效使整体不合格 | 成长可靠性未通过，不宜进行最终平衡判定 | 高开局压力叠加强化失效，整体不合格 |

## 调整顺序与后续验收

1. 修复肉鸽全部选卡写回；补充真实选卡路径测试，而不仅测试纯函数。
2. 接通敌弹与 Boss 难度参数；先限定倍率上限，不能直接把缺失的高波次弹速倍率全部叠上，否则后期可能骤然增难。
3. 简单 Boss 提供更长预警、休整和较低耐久；中等作为基准；困难强化攻击节奏但保留读招窗口。
4. 使用相同种子重跑本审计，增加会识别弹墙通道/主动捡道具的脚本、多个合理构筑和自然成长到后期的场景。
5. 真人覆盖触屏/鼠标/键盘、闯关全部十关、无限与肉鸽至少一个完整 Boss 轮换，记录失败原因和耗时，之后再给出“符合难度定位”的最终结论。

## 复现与数据

运行 `node test/balance.audit.js`。完整每条模拟在 `test/reports/balance-results.json`，参数实测在 `balance-probes.json`，逐项选卡检查在 `balance-perks.json`。这些是审计数据，不以固定生存率作为单元测试断言。
