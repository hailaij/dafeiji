# v1.5.0 安装包实机测试

测试日期：2026-09-24。测试对象为已发布的安装包，未重新编译或注入测试代码。

## 环境与安装包

- Android：OPPO A93s 5G（型号 PFGM00），Android 12，WebView 151.0.7922.169，物理分辨率 1080×2400。
- Windows：Windows 11 专业版，系统版本 10.0.26200，实际启动单文件 EXE / WebView2。
- APK SHA-256：`9ac11597e176ec66cf9e4e32356efcf95d114c1eaa0e524984851c618d46dab7`。
- EXE SHA-256：`95c325a6f78262261940f2c1849a519fe53f0d94eabb59e101a065ce961e923f`。
- Android 确认为 versionName 1.5.0、versionCode 12。Windows 解压的所有 JS、首页、manifest 与离线缓存文件和发布源码逐字节一致。

## Windows 已完成

- 单文件直接启动、WebView2 初始化、首页和自动试射正常；该 EXE 是便携程序，没有安装向导。
- 实际切换为壁垒 + 扇形，确认机体和武器独立选择。
- 无限模式中等难度进入战斗，显示初始护盾 1、生命 3，正常射击、得分并推进到第 2 波。
- E 键释放 EMP，显示 10 秒冷却；P 键暂停。
- 重新开始后分数归零、回到第 1 波、基础火力和护盾恢复。
- 返回菜单、正常退出、重新运行 EXE 后仍保留壁垒 + 扇形搭配。
- WebView2 显示音频正在播放；未做人工音质判断。
- 测试结束正常退出，无崩溃对话框。

## Android 已完成

- ADB 首次安装成功；安装前未检测到旧版包，不能视为从 1.4.2 升级验证。
- 首次启动成功，首页、难度、三模式选择和机库正常显示。
- 用户实际操作灵翼 + 重弹，在肉鸽中等难度推进到第 4 波；设备截图与 UI 树可见对应机体、火力、血量与波次。
- 用户在真机上确认：一指持续拖动飞机，另一指点击 EMP，可以同时移动并释放。这项结论来自用户操作反馈，不是 ADB 多触点注入。
- 闯关模式启动、暂停正常；模拟桌面启动图标（MAIN / LAUNCHER / 0x10200000）从后台恢复，保留原来的关卡和暂停状态。
- 抽查该应用进程日志，未发现 FATAL EXCEPTION、AndroidRuntime 崩溃或 Uncaught 脚本错误。

- 继续后可以重新暂停；重新开始后分数归零；返回菜单和无限模式进入战斗、得分正常。
- 对同一 v1.5.0 APK 再次执行保留数据覆盖安装成功；随后 COLD 启动成功（系统 Activity TotalTime 1037 ms，不代表完整可交互耗时）。
- 等待页面加载后确认仍为灵翼 + 集中重弹，证明本次同版本覆盖安装与冷启动保留了机库存档。
- 测试结束停留在手机主菜单，保留安装与用户数据。

## 结论

本次两台设备的安装/启动及所列核心功能冒烟测试通过，未复现阻断运行的问题。窗口最大化和未列出的场景不在通过结论中。未改动已发布程序、未替换 Release 资产；本报告是发布后的补充证据。

## 边界

这是两台实际设备上的安装与功能冒烟测试，不是全机型兼容认证。未覆盖旧版到新版的升级、不同厂商系统、长时间温升/耗电、完整通关、断网切换与主观音质。不将此前浏览器模拟或平衡脚本结果冒充真机结果。

Windows 最大化测试被自动化工具的窗口几何信息错误阻断，未记为通过。Android 战斗中 UI 自动化曾因动画持续变化无法获得 idle 状态，后续通过截图和暂停后的新 UI 树验证，未把旧 UI 树作为成功证据。
